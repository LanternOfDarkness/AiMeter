<UserControl x:Class="AiMeter.Controls.CircularProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="{Binding Size, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Height="{Binding Size, RelativeSource={RelativeSource AncestorType=UserControl}}">
    <Grid>
        <!-- Background Track -->
        <Ellipse Stroke="{Binding TrackBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 Margin="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <!-- Foreground Track -->
        <Path Stroke="{Binding ForegroundBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeEndLineCap="Round"
              StrokeStartLineCap="Round">
            <Path.Data>
                <PathGeometry>
                    <PathFigure x:Name="PART_PathFigure" StartPoint="0,0">
                        <ArcSegment x:Name="PART_ArcSegment" SweepDirection="Clockwise" />
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>
        
        <!-- Center Text -->
        <Viewbox Margin="15">
            <TextBlock Text="{Binding ValueText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="{Binding ForegroundBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontWeight="Bold" />
        </Viewbox>
    </Grid>
</UserControl>
