<Window x:Class="AiMeter.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:AiMeter.ViewModels"
        mc:Ignorable="d"
        Title="AiMeter Settings" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#202020">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Widget Opacity -->
        <TextBlock Grid.Row="0" Text="Widget Opacity" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Slider Minimum="0.1" Maximum="1.0" Value="{Binding Config.WidgetOpacity}" Width="250" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Config.WidgetOpacity, StringFormat={}{0:P0}}" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        
        <!-- Authentication -->
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <Button Content="Log into Claude" Command="{Binding LoginToClaudeCommand}" Width="150" Height="30" HorizontalAlignment="Left" Background="#5B5B5B" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" />
                <TextBlock Text="{Binding LoginStatusText}" Foreground="{Binding LoginStatusColor}" VerticalAlignment="Center" Margin="15,0,0,0" FontWeight="Medium" />
            </StackPanel>
            <TextBlock Text="Requires logging into Claude.ai to fetch web limits." Foreground="#888" FontSize="11" Margin="0,0,0,0" />
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save &amp; Close" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="120" Height="30" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
